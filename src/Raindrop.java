import processing.core.PImage;

import java.util.List;

public class Raindrop extends AbstractEntity {
    public Raindrop(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }

}
